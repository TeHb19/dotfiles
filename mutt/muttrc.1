set header_cache=~/.cache/mutt/school

set imap_user="`cat ~/secrets/mailuser1 | sed 's/@.*//'`"
set imap_pass="`secret-tool lookup user $(cat ~/secrets/mailuser1) type mail`"
set folder="`cat ~/secrets/imap1`"

set smtp_url="`cat ~/secrets/smtp1`"
set smtp_pass=$imap_pass

set spoolfile = +Inbox
set postponed = +Draft
set record = +Sent
set realname = 'Vladimir Rogovskiy'
set from="`cat ~/secrets/mailuser1`"
set ssl_force_tls=yes
set ssl_starttls=yes

unset imap_passive
set imap_keepalive=300
set mail_check=60
